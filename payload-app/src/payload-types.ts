// This file is a minimal stub to unblock the build.
// It will be regenerated properly later.

export type User = {
  id: string
  email: string
  role?: string
}

// Add any other types that might be referenced by your collections.
// For now, we export a generic type to satisfy the imports.
export type Collection = any
export type Field = any
export type Access = any
export type Where = any
